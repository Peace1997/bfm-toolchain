# 贡献指南

这份文档是骨架先行的：结构已经定了，内容在陆续填。欢迎补内容、修错字、提问题。

## 最快的参与方式

每个未完成的页面顶部都有一个「待补充」框，里面明确列了这一页缺什么。挑一条你知道答案的，补上，提 PR。

站点底部每页都有「编辑此页」链接，直接跳到 GitHub 的编辑界面，改完提 PR 即可——不用把仓库拉到本地。

## 本地预览

```bash
npm install && npm run start
```

提 PR 前跑一次构建，死链和 MDX 语法错误会在这里暴露：

```bash
npm run build
```

CI 会对每个 PR 跑同样的构建，不过本地先跑一遍能省一轮等待。

## 内容边界

> [!CAUTION]
> 文档的分享范围随时可能扩大。写进仓库的东西一旦流出去就收不回来，
> 所以从第一天起就按「会被更多人看到」的标准来写。

提交前请对照检查，以下内容**不要**进这个仓库：

- 内网地址、IP、端口、跳板机信息
- 模型权重的内部下载链接、对象存储凭证、任何形式的密钥
- 未发布本体的硬件参数、关节限位的具体数值
- 客户名称、项目代号、未公开的合作信息
- 未发布的性能数据、内部评测结论
- 采集数据中包含的人脸、场地等可识别信息

可以公开的是「怎么用」：流程、格式约定、概念解释、CLI/API 用法、公开本体的接入方式。

拿不准就先提 Issue 问，别直接提 PR。

## 写作约定

**一页只讲一件事。** 页面长了就拆，不要在一页里塞完整个模块。

**术语以 [核心概念](docs/overview/concepts.mdx) 为准。** 需要新术语先加到那一页，再在别处使用。

**标注运行环境。** 涉及具体操作时说清楚是仿真还是真机：

```html
<span className="badge-sim">仿真</span>
<span className="badge-real">真机</span>
<span className="badge-model">模型</span>
<span className="badge-todo">待补充</span>
```

**数值必须可追溯。** 写进文档的时延、成功率、显存占用，要么是你实测的，要么注明出处。没实测过就写 `<span className="badge-todo">待补充</span>`，不要填一个"大概是这个数"。

**内容补完后**，把页面顶部的 `<Stub>` 块删掉，并在 [文档进度](docs/overview/roadmap.mdx) 里更新状态。

## 提交信息

用中文或英文都行，说清楚改了什么即可。建议加个前缀：

```text
docs(datasets): 补齐观测字段表
fix: 修正快速开始里的死链
feat(home): 首页加上技能演示视频
```
